# 公開前チェックリスト

## 1. ソリューションメッセージ検証

- [x] `AS400 / COBOL Migration` を QueryPie のソリューションまたはサービスとして明記可能
- [x] 主表記は `IBM i（AS/400）モダナイゼーション` とし、`AS400 / COBOL マイグレーション` は SEO と文脈上の補助表記として使用
- [x] AI ベースの COBOL/RPG 分析、設計書生成、テストケース生成、Java/API/Cloud 移行は十分に公開可能
- [x] DB2 / Oracle 分析、PostgreSQL 移行、Linux/OCI/AWS 移行を具体的に言及
- [x] Payroll 事例の具体的な期間表現は一般的なホームページ公開文言では使用しない
- [x] 診断、PoC、変換、実装、運用安定化までサービス範囲として明記

## 2. 日本語品質レビュー

- [ ] 日本語ネイティブまたは日本市場向けマーケティング担当者によるレビューは最終公開前の後続レビューとして実施
- [x] `AS/400`、`IBM i`、`COBOL`、`RPG`、`レガシーシステム`、`基幹システム`、`モダナイゼーション` の表記方針を設定
- [x] 過度な営業表現を避ける方針を設定
- [x] 日本のB2B技術サービスページのトーンに合わせ、落ち着いた短めの文を中心に作成
- [x] `AS400` は URL、SEO 補助キーワード、検索文脈で限定的に使用し、本文の基本表記は `IBM i（AS/400）` または `AS/400` を優先

## 3. SEOレビュー

- [x] H1 とメタタイトルに `IBM i（AS/400）モダナイゼーション` を含める
- [x] メタタイトルに `AS400 / COBOL マイグレーション` を補助キーワードとして含める
- [x] H2/H3 に `COBOL マイグレーション`、`RPG`、`基幹システム`、`レガシーシステム刷新`、`2025年の崖` など関連キーワードを含める
- [x] メタタイトル案を作成
- [x] メタディスクリプション案を作成。会社名は `QueryPie AI` と表記
- [ ] 日本向けWebサイト内の関連ページへのリンク追加は、今回のコンテンツ初稿では行わず後続の公開・露出段階で再検討
- [x] 相談／問い合わせ／デモ依頼CTAの基本パスは `/contact-us` を使用

## 4. 公開位置の決定

- [x] 一次公開形式は日本向けWebサイトの静的Webページに決定
- [x] 静的Webページは `/services/` 配下のサービスページとして置く初期計画を設定
- [x] White paper 展開の有無は後続で検討
- [x] Blog 展開の有無は後続で検討
- [x] `corp-web-japan` のコンテンツ構造を確認
- [x] 静的ページ実装方式は route-local `page.tsx` を中心とし、視覚コンポーネントは `src/components/sections/as400-cobol/` 配下に分離
- [x] `?lang=ko` リクエスト時の韓国語ページは `src/app/services/as400-cobol/page.ko.tsx` で route-local authoring として分離レンダリング
- [x] スラッグは `/services/as400-cobol` に決定
- [x] canonical URL は `https://querypie.ai/services/as400-cobol` に設定
- [x] コンテンツ初稿段階では production/stage へのデプロイ可能性を許容しつつ、robots は `noindex, nofollow` に設定
- [x] ナビゲーション露出と sitemap 露出は後続作業として分離

## 5. 根拠と表現リスク

- [ ] 日本の IBM i(AS/400) 顧客が約15,000社以上という主張の出典を確認
- [ ] 約4,000〜5,000社が COBOL/AS400 を使用しているという主張の出典を確認
- [ ] METI `2025年の崖` と年間最大12兆円損失表現の原文出典を確認
- [x] コンテンツ初稿では数値を可能な範囲で示し、出典検証は別途後続作業として実施
- [x] 出典検証前の数値表現は断定を避け、`とされ`、`といわれています` などの留保表現を使用
- [x] Payroll、Hitachi など特定顧客・ベンダー名は公開ページで使用しない
- [x] 顧客名、ベンダー名、プロジェクト名は匿名化し、AS400、AS/400、IBM i、COBOL、RPG などの技術名称は明示
- [x] 匿名化された顧客事例と実際の運用シナリオは含めてよい
- [x] 「日本は世界で唯一...」のような断定表現は使用しない

## 6. 画像とアセット

- [x] Hero 画像は生成型 bitmap 画像として制作
- [x] Hero 画像は AS/400、COBOL をモダナイゼーションする流れを視覚化
- [x] 実際の AS/400/IBM i 機器を直接描写していると断定されないよう、抽象的な視覚表現を使用
- [x] 本文には段階的 Migration の流れを図とカード／パネルの混合型で実装
- [x] alt テキストを日本語で作成
- [x] 画像パスは `public/services/as400-cobol/` 配下の route-aligned asset として整合

## 7. 実装前確認

- [x] CTA の基本URLは `/contact-us` として確定
- [x] 公開パスは `/services/as400-cobol` に確定
- [x] sitemap への含有はコンテンツ完成後の公開露出段階で対応。今回の初稿実装では含めない
- [x] 関連テスト範囲は `npm run test:ci`、`npm run build`、ブラウザ直接確認
- [x] デプロイ前後は直接URLで表示確認。ナビゲーション／sitemap 露出は後続

## 8. 残りの質問

1. ナビゲーション露出は後続作業。露出時は `サービス` 配下の FDE メニュー下に置く方向で検討
2. sitemap 露出はコンテンツ完成後の公開露出段階で対応
3. `/contact-us` に付与する追加 query string stable key 設計は後続作業
4. 日本 IBM i(AS/400)、COBOL/AS400、METI `2025年の崖` の数値主張の出典確認は別途実施
5. White paper と Blog 展開は後続で検討
