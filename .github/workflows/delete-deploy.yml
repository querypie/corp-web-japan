name: Delete Deploy

permissions:
  contents: read

on:
  delete:

jobs:
  delete-deploy:
    name: Delete Deploy
    runs-on: ubuntu-latest
    if: github.event.ref_type == 'branch'
    steps:
      - name: No-op
        run: echo "Vercel Git integration manages preview cleanup automatically."
