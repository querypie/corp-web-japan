"use client"

// Stubbed for mockup — real debug panel is dev-only and not needed for landing demo.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function BlockDebugPanel(props: {
  blockId?: string
  sessionId?: string
}): null {
  return null
}
